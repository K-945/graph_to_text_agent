# graph_to_text_agent

`Graph.py` and `vertical.py` are programs used to construct complete graphs.

`TGAgent` is a program that reads data build graphs and reads them.

`paperqa.jsonl` is the test data.
